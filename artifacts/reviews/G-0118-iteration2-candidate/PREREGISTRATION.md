# G-0118 iteration-2 candidate — independent red-team preregistration

Registered before implementing or running the review verifier.

## Pinned subject

The sole candidate under review is
`artifacts/math/G-0118/prefix_exact_cegis_iteration2_v1.json`, expected
SHA-256
`1d3fd50449fd63c0f8d795cb4d1428fd7a89ef97bcd709c01c579115ea8ccb4b`.
The review will hash, but will neither import nor execute, the iteration-2
producer.  No G-0117 or G-0118 producer/result file may be modified.

## Consumer, gate, defect class, and retirement

- **Consumer:** the research lead's promotion gate for this exact candidate.
- **Gate:** the candidate cannot be treated as more than a finite-row member
  unless an independent review first confirms its exact 314-row receipt and
  then dispositions the complete ordered-cone normal form.
- **Observed defect class:** iteration 1 satisfied every frozen finite row but
  failed globally at the next lexicographic hinge direction.
- **Retirement condition:** this review retires when the pinned candidate is
  either globally refuted by a nonzero complete replay residual, or survives
  complete two-prime screening and is handed onward for full exact-integer
  global replay.  Two-prime zero alone never retires the identity obligation.

## Independent procedure

1. Reconstruct the 301 panel coordinates from the frozen signed-i128 cache
   prefix or the retained-column records, and reconstruct the eleven linear
   plus two hinge coordinates from their frozen streams.
2. Re-derive the support from the common 115-column panel basis and the eight
   rank-growing trial sequences.  Check canonical sparse projection, sequence
   ordering/uniqueness, every file binding, prefix hash, coordinate stream
   hashes, and the selected-basis row-major signed-i128 digest.
3. Accumulate all 314 rows with arbitrary-precision integers against the
   independently assembled target.  Add one to each nonzero coefficient in
   turn and require every mutant to fail.  Also reject sequence-map, support,
   target-scale, target-factorial, target-coordinate, row-order, and hash
   mutations.
4. Independently rebuild each selected atom's full ordered-cone normal form
   from its signed loopless graph.  A subset dynamic program counts all
   labelled permutations exactly.  Aggregate every active primitive hinge and
   all eleven linear coordinates modulo 1,000,000,007 and 1,000,000,009.
5. If either prime has a nonzero residual, report the lexicographically first
   residual direction and independently recompute that single coordinate over
   arbitrary-precision integers using the specification's active-injection
   coordinate DP.  If both primes are globally zero, notify the research lead
   immediately and stop before any identity claim.

The global normal-form implementation owes a literal-permutation differential
self-test, orientation and linear-correction tests, relabelling invariance,
and a planted equality-destroying edge mutant.  A self-test that cannot fail
is not evidence.

## No-claim boundary

Exact agreement on 314 rows is finite membership only, not MAX11.  Modular
zero is a screen only.  A nonzero modular residual refutes only this exact
cleared rational candidate; it does not prove fixed-family nonmembership,
family completeness, an unrestricted two-hidden-layer lower bound, or the
all-arity target.
