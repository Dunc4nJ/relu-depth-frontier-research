# G-0113b preregistration — full arbitrary-endpoint double-star potency

Registered after the incidence-slot null and before writing or running the
G-0113b outcome producer.  This is a new family, not a retry of the failed
one: each branch may attach the new vertex to any unordered multiset of two
vertices, whether or not those vertices coincide, occur in that branch, or
form an existing source edge.

For every MAX6 source atom `(L_T,R_T)`, put `w=7`.  For every
`0 <= a <= b <= 6` and `0 <= c <= d <= 6`, include independently

```text
Sym_7 max(h_{L_T + {(a,w),(b,w)}},
          h_{R_T + {(c,w),(d,w)}}).
```

There are exactly `4 * binom(8,2)^2 = 3,136` RAW columns.  Duplicates may be
collapsed only after their complete ordered-cone normal forms are proved
identical; the first raw descriptor represents each duplicate class.  A TIED
diagnostic also forms all `28^2=784` exact source-coefficient combinations.

The frozen normal-form engine from `double_star_search.py` may be reused only
if its SHA-256 remains
`b92ef70ab9187c4989c80cf7b948324ec65a9ef8deff9cf3e5dfdff022a106ea`.
It must rerun the exact MAX6 and MAX7 certificate controls and literal orbit
checks.  The already completed brute-force replay remains method-disjoint
support for that engine but is not T2 review.

Compute the complete dominant-ordered-cone integer matrices and exact ranks
over `Q`, target last.  If RAW excludes MAX7, emit an exact primitive integer
separator and stop.  If RAW contains MAX7, select the target-last
lexicographic basic solution on the first representative of every semantic
class and replay every linear and hinge coordinate exactly.  Report all
nonzero coefficients with original raw descriptors.  Apply the same rule to
TIED as a transport diagnostic.

No MAX11 execution is authorized by this file.  A lower-arity pass only
establishes potency and motivates a separately preregistered MAX11 search.
Neither a pass nor a miss says anything about unrestricted two-hidden-layer
networks.  A miss excludes only this 3,136-column lower family; a pass is an
explicit alternative MAX7 identity inside it, not a transported MAX11
identity.
