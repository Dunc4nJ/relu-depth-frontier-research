# V-0008 request — MAX_11 exact-certificate review

Review claim `C-0002@2` from a fresh context under the locally attributed
Claude Opus lineage. Treat the exact certificate as adversarial input.

Required checks:

1. Audit the pinned upstream certificate semantics and the translation from
   the run7 universe witness.
2. Build `tools/verify11` independently and run exact `verify` on the complete
   15,896-term certificate.
3. Compare 20 seeded translated terms against the source universe and compare
   their literal `11!`-permutation columns with the dynamic-program columns.
4. Recompute the full exact identity in an implementation that imports no
   `tools/verify11` code.
5. Run the method-disjoint exact lattice falsifier on both `{0,1}^11` and
   `{0,1,2}^11` profiles, retaining a repeated run.
6. Treat any schema mismatch, nonzero exact residual, translation mismatch, or
   lattice disagreement as a negative finding. Keep finite-point agreement
   explicitly non-probative for global equality.

The requested statement is n=11 only. Do not infer anything for n>=12, all n,
minimal support, human review, or proof-assistant formalization.
