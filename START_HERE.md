# Start here

The target is the unrestricted, exact, global two-hidden-layer ReLU representation of `MAX_n`. MAX11 is the first unresolved rung in the campaign's dated retrieved frontier, not an unbounded priority claim. Read the exact quantifiers before touching code.

Run:

    source scripts/activate-toolchain.sh
    scripts/verify-toolchain.sh
    ./skill-runtime verify-quick

Then inspect `STATUS.md` and claim exactly one active object from `beads/QUEUE.md`. Imported 2026-08-27 analyses are quarantined hints. Reproduce known MAX5–MAX10 controls before treating their counts, verifier, or ansatz as native evidence.

The root research lead delegates only bounded tasks to native subagents and personally adjudicates every result. A good handoff names: object/route IDs, exact files and commit, contamination boundary, what changed epistemically, commands and results, failed paths, remaining falsifier, and the smallest next discriminator. Same-family subagents count at most as T1 review.
